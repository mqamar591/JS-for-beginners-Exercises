var usernames = ["admin", "username591", "qamarsharif19", "informators19", "qamarmeta"];

txt=[];
for (let i=0; i < usernames.length; i++) {
if (usernames[i]==usernames[0]) {
    console.log(txt = "Hello " + usernames[0] + " would you like to see a status report?")
}
if (usernames[i]==usernames[1]) {
    console.log(txt = "Hello " + usernames[1] + " thanks for logging again?")
}
if (usernames[i]==usernames[2]) {
    console.log(txt = "Hello " + usernames[2] + " thanks for logging again?")
}
if (usernames[i]==usernames[3]) {
    console.log(txt = "Hello " + usernames[3] + " thanks for logging again?")
}
if (usernames[i]==usernames[4]) {
    console.log(txt = "Hello " + usernames[4] + " thanks for logging again?")
}


}

console.log(txt);

